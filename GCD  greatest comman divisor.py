import math
n=int(input())
for i in range(n, 0, -1):
    x=(input())
    y=(input())
    x=int(x)
    y=int(y)
    def findGcd(self,x,y):
        self.x=x
        self.y=y
    for j in range (1): 
        gcd = math.gcd(x, y)
        print(gcd)